workflows:
  build_android:
    name: Build Android APK
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter analyze
      - flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk
